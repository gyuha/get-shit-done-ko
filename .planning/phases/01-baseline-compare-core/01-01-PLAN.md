---
phase: 01-baseline-compare-core
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-upstream-release.cjs
  - skills/gsd-sync-upstream/scripts/check-upstream-release.cjs
  - skills/gsd-sync-upstream/references/context.md
autonomous: true
requirements:
  - SYNC-01
  - SYNC-02
user_setup: []
must_haves:
  truths:
    - Compare logic reads `get-shit-done/UPSTREAM_VERSION` as the sync eligibility source of truth.
    - Compare results distinguish `current`, `update_available`, and `ahead` without relying on fork `package.json` as the decision source.
    - The skill bundle helper and repo-local helper expose the same compare contract.
  artifacts:
    - path: scripts/check-upstream-release.cjs
      provides: normalized compare state for maintainer sync decisions
    - path: skills/gsd-sync-upstream/scripts/check-upstream-release.cjs
      provides: bundled helper aligned with repo-local compare logic
    - path: skills/gsd-sync-upstream/references/context.md
      provides: maintainer-facing source-of-truth and preserved-path policy
  key_links:
    - from: scripts/check-upstream-release.cjs
      to: get-shit-done/UPSTREAM_VERSION
      why: baseline file must drive sync eligibility
    - from: skills/gsd-sync-upstream/scripts/check-upstream-release.cjs
      to: scripts/check-upstream-release.cjs
      why: bundled and repo-local compare behavior must stay aligned
---

<objective>
Lock the compare contract around tracked upstream baseline and latest release metadata.

Purpose: Prevent false sync decisions caused by using fork package version or drifted helper implementations.
Output: Aligned compare helpers and maintainer reference text for baseline source-of-truth behavior.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-baseline-compare-core/01-RESEARCH.md

@scripts/check-upstream-release.cjs
@skills/gsd-sync-upstream/scripts/check-upstream-release.cjs
@skills/gsd-sync-upstream/references/context.md
@docs/UPSTREAM-SYNC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize compare source-of-truth rules</name>
  <files>scripts/check-upstream-release.cjs, skills/gsd-sync-upstream/scripts/check-upstream-release.cjs</files>
  <read_first>get-shit-done/UPSTREAM_VERSION, docs/UPSTREAM-SYNC.md, skills/gsd-sync-upstream/references/context.md</read_first>
  <action>Review the repo-local compare helper and the bundled skill helper. Make the baseline-file-first contract explicit in code comments, exported state shape, and any argument validation. Keep the normalized output fields stable: `current_tag`, `latest_tag`, `latest_published_at`, `package_version`, `update_available`, `local_ahead`, `status`. Do not let `package_version` drive sync eligibility logic.</action>
  <verify>node scripts/check-upstream-release.cjs --current-file get-shit-done/UPSTREAM_VERSION --json</verify>
  <acceptance_criteria>
    - `scripts/check-upstream-release.cjs` still emits `status`, `update_available`, and `local_ahead`
    - skill-bundled `check-upstream-release.cjs` matches the same contract or intentionally reuses the repo-local helper
  </acceptance_criteria>
  <done>Both helper locations clearly encode the same baseline-first compare contract.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten maintainer compare reference</name>
  <files>skills/gsd-sync-upstream/references/context.md</files>
  <read_first>skills/gsd-sync-upstream/SKILL.md, docs/UPSTREAM-SYNC.md</read_first>
  <action>Update the maintainer context reference so downstream workflow steps unambiguously state that `get-shit-done/UPSTREAM_VERSION` is the only sync eligibility source of truth, `package.json` is informational only, and `current`/`ahead`/`update_available` each imply distinct next actions.</action>
  <verify>rg -n "source of truth|package.json|current|ahead|update_available" skills/gsd-sync-upstream/references/context.md</verify>
  <acceptance_criteria>
    - reference text mentions `get-shit-done/UPSTREAM_VERSION`
    - reference text explicitly states `package.json` is not the decision source
  </acceptance_criteria>
  <done>The maintainer reference matches the compare helper contract and no-op policy.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node scripts/check-upstream-release.cjs --current-file get-shit-done/UPSTREAM_VERSION --json` succeeds
- [ ] `node --test tests/upstream-sync.test.cjs` still passes
- [ ] bundled helper and repo-local helper expose the same compare-state semantics
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Compare logic uses tracked baseline file as the only sync eligibility source of truth
  </success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-compare-core/01-01-SUMMARY.md`
</output>
