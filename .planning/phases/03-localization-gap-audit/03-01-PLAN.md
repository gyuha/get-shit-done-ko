---
phase: 03-localization-gap-audit
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/audit-localization-gap.cjs
  - skills/gsd-sync-upstream/scripts/audit-localization-gap.cjs
  - tests/localization-gap-audit.test.cjs
  - skills/gsd-sync-upstream/SKILL.md
  - docs/UPSTREAM-SYNC.md
autonomous: true
requirements:
  - L10N-01
user_setup: []
must_haves:
  truths:
    - Localization audit can produce a file-level changed manifest from baseline/upstream/current snapshots.
    - Repo-local and bundled audit helpers expose the same JSON contract for changed files and overlay deltas.
    - Maintainer docs describe the changed manifest as the input to translation review.
  artifacts:
    - path: scripts/audit-localization-gap.cjs
      provides: file-level changed manifest and overlay delta audit
    - path: tests/localization-gap-audit.test.cjs
      provides: deterministic regression coverage for audit manifest output
    - path: skills/gsd-sync-upstream/SKILL.md
      provides: maintainer-facing localization audit step after sync
  key_links:
    - from: scripts/audit-localization-gap.cjs
      to: scripts/apply-upstream-refresh.cjs
      why: audit should reuse the same snapshot/import surface rules as safe apply
    - from: skills/gsd-sync-upstream/SKILL.md
      to: docs/UPSTREAM-SYNC.md
      why: skill flow and runbook must describe the same audit entrypoint
---

<objective>
Create a deterministic file-level manifest for localization audit inputs.

Purpose: Give maintainers a precise list of changed files and overlay deltas to review after upstream sync.
Output: Audit helper, tests, and docs aligned on changed file manifest output.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-localization-gap-audit/03-RESEARCH.md

@scripts/apply-upstream-refresh.cjs
@skills/gsd-sync-upstream/SKILL.md
@docs/UPSTREAM-SYNC.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a localization audit helper with file-level changed manifest output</name>
  <files>scripts/audit-localization-gap.cjs, skills/gsd-sync-upstream/scripts/audit-localization-gap.cjs, tests/localization-gap-audit.test.cjs</files>
  <read_first>scripts/apply-upstream-refresh.cjs, AGENTS.md, tests/upstream-sync.test.cjs</read_first>
  <action>Create `scripts/audit-localization-gap.cjs` and a bundled mirror in `skills/gsd-sync-upstream/scripts/`. Reuse the same snapshot and import-surface rules as `apply-upstream-refresh.cjs`. The audit helper must emit JSON fields `current_tag`, `incoming_tag`, `apply_mode`, `changed_files`, `overlay_reapply`, `overlay_delete`, and `translation_candidates`. `changed_files` must be file-level paths computed from baseline vs upstream snapshot diffs. `translation_candidates` must be the subset of changed files under `docs/`, `README.md`, `agents/`, and `commands/` that likely need localization review. Add deterministic tests using fixture directories and exact path assertions.</action>
  <verify>node --test tests/localization-gap-audit.test.cjs</verify>
  <acceptance_criteria>
    - `tests/localization-gap-audit.test.cjs` asserts exact `changed_files` and `translation_candidates` entries
    - `scripts/audit-localization-gap.cjs --json` includes `changed_files` and `translation_candidates`
    - `diff -u scripts/audit-localization-gap.cjs skills/gsd-sync-upstream/scripts/audit-localization-gap.cjs` exits 0
  </acceptance_criteria>
  <done>The audit helper produces a deterministic file-level manifest that downstream localization checks can consume.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the changed manifest into maintainer workflow docs</name>
  <files>skills/gsd-sync-upstream/SKILL.md, docs/UPSTREAM-SYNC.md</files>
  <read_first>skills/gsd-sync-upstream/SKILL.md, docs/UPSTREAM-SYNC.md, scripts/audit-localization-gap.cjs</read_first>
  <action>Update the skill and runbook so after dry-run/apply maintainers can run `node scripts/audit-localization-gap.cjs --to-tag <latest_tag> --mode source-of-truth --json` and review `changed_files`, `overlay_reapply`, `overlay_delete`, and `translation_candidates`. Keep the existing compare/dry-run/apply flow intact and place the audit step before canonical validation.</action>
  <verify>rg -n "audit-localization-gap|changed_files|translation_candidates|overlay_delete" skills/gsd-sync-upstream/SKILL.md docs/UPSTREAM-SYNC.md</verify>
  <acceptance_criteria>
    - both docs mention `audit-localization-gap.cjs`
    - both docs list `changed_files` and `translation_candidates` as review fields
  </acceptance_criteria>
  <done>Maintainers know exactly how to generate and read the localization audit manifest.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test tests/localization-gap-audit.test.cjs` passes
- [ ] `diff -u scripts/audit-localization-gap.cjs skills/gsd-sync-upstream/scripts/audit-localization-gap.cjs` exits 0
- [ ] `rg -n "audit-localization-gap|changed_files|translation_candidates" skills/gsd-sync-upstream/SKILL.md docs/UPSTREAM-SYNC.md` returns aligned wording
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Maintainers can generate a deterministic changed-file manifest for localization audit
  </success_criteria>

<output>
After completion, create `.planning/phases/03-localization-gap-audit/03-01-SUMMARY.md`
</output>
