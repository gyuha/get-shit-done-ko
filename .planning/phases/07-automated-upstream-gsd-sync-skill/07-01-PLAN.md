---
phase: 07-automated-upstream-gsd-sync-skill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .codex/skills/gsd-sync-upstream/SKILL.md
  - scripts/check-upstream-release.cjs
  - get-shit-done/UPSTREAM_VERSION
  - docs/UPSTREAM-SYNC.md
autonomous: true
requirements:
  - L10N-04
  - L10N-06
user_setup: []
must_haves:
  truths:
    - Maintainers can run a dedicated skill that compares upstream GitHub release state against a tracked local baseline before touching repo files
    - The repo has a machine-readable upstream baseline source that no longer depends on parsing only prose documentation
  artifacts:
    - .codex/skills/gsd-sync-upstream/SKILL.md
    - scripts/check-upstream-release.cjs
    - get-shit-done/UPSTREAM_VERSION
    - docs/UPSTREAM-SYNC.md
  key_links:
    - `gsd-sync-upstream` uses the same tracked baseline that `docs/UPSTREAM-SYNC.md` documents
    - compare/no-op output shows upstream latest tag/date alongside local baseline and does not rely on `package.json` alone
---

<objective>
Create the decision point for safe upstream syncing before any import logic lands.

Purpose: establish a maintainer-only skill, a machine-readable baseline, and GitHub-release comparison behavior that can say "update", "no-op", or "ahead" with exact version/date evidence.
Output: a dedicated upstream-sync skill scaffold plus baseline/compare plumbing.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-automated-upstream-gsd-sync-skill/07-CONTEXT.md
@.planning/phases/07-automated-upstream-gsd-sync-skill/07-RESEARCH.md
@.planning/phases/07-automated-upstream-gsd-sync-skill/07-VALIDATION.md
@docs/UPSTREAM-SYNC.md
@package.json
@.release-monitor.sh
@.codex/skills/gsd-update/SKILL.md
@get-shit-done/workflows/update.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce machine-readable upstream baseline tracking and GitHub release comparison</name>
  <files>scripts/check-upstream-release.cjs, get-shit-done/UPSTREAM_VERSION, docs/UPSTREAM-SYNC.md</files>
  <read_first>docs/UPSTREAM-SYNC.md, package.json, .release-monitor.sh, .planning/phases/07-automated-upstream-gsd-sync-skill/07-CONTEXT.md, .planning/phases/07-automated-upstream-gsd-sync-skill/07-RESEARCH.md</read_first>
  <action>Implement decisions D-03 through D-05 by adding a machine-readable local upstream baseline file (for example `get-shit-done/UPSTREAM_VERSION`) and a helper script `scripts/check-upstream-release.cjs` that can fetch the latest `gsd-build/get-shit-done` release metadata, compare it semantically to the tracked local baseline, and emit JSON/text output with `current`, `latest`, `latest_tag`, `latest_published_at`, and `update_available`. Update `docs/UPSTREAM-SYNC.md` so the same baseline source is documented for maintainers. Do not use `package.json` as the sole comparison key.</action>
  <verify>test -f get-shit-done/UPSTREAM_VERSION && test -f scripts/check-upstream-release.cjs && node scripts/check-upstream-release.cjs --current-file get-shit-done/UPSTREAM_VERSION --latest-tag v999.0.0 --json | rg -n "\"update_available\":true|\"latest_tag\":\"v999.0.0\"" && rg -n "UPSTREAM_VERSION|tracked baseline|GitHub releases" docs/UPSTREAM-SYNC.md</verify>
  <acceptance_criteria>
    - `get-shit-done/UPSTREAM_VERSION` exists and contains the tracked upstream baseline
    - `scripts/check-upstream-release.cjs` can compare a local baseline against a supplied or fetched upstream tag and print JSON
    - `docs/UPSTREAM-SYNC.md` documents the same baseline source the script reads
  </acceptance_criteria>
  <done>Maintainers have a stable local baseline and a reusable compare helper instead of ad-hoc prose parsing.</done>
</task>

<task type="auto">
  <name>Task 2: Add the dedicated maintainer skill and explicit no-op reporting</name>
  <files>.codex/skills/gsd-sync-upstream/SKILL.md, scripts/check-upstream-release.cjs</files>
  <read_first>.codex/skills/gsd-update/SKILL.md, get-shit-done/workflows/update.md, scripts/check-upstream-release.cjs, .planning/phases/07-automated-upstream-gsd-sync-skill/07-CONTEXT.md</read_first>
  <action>Implement D-01, D-02, and D-09 by creating `.codex/skills/gsd-sync-upstream/SKILL.md` as a maintainer-only skill that first runs the release comparison helper, shows upstream latest tag/date plus local baseline and fork package version, and exits cleanly with a no-op message when upstream is not newer. Make the wording clearly distinct from runtime self-update so users do not confuse repo sync with `$gsd-update`.</action>
  <verify>test -f .codex/skills/gsd-sync-upstream/SKILL.md && node scripts/check-upstream-release.cjs --current-tag v1.28.0 --latest-tag v1.28.0 --json | rg -n "\"update_available\":false|\"latest_tag\":\"v1.28.0\"" && rg -n "maintainer|GitHub release|no-op|package version" .codex/skills/gsd-sync-upstream/SKILL.md</verify>
  <acceptance_criteria>
    - `.codex/skills/gsd-sync-upstream/SKILL.md` exists
    - the skill explicitly states it syncs the current repo's vendored GSD rather than updating runtime installs
    - no-op output includes exact compared versions and dates when upstream is not newer
  </acceptance_criteria>
  <done>The repo now has a dedicated upstream-sync entrypoint with safe decision reporting.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f get-shit-done/UPSTREAM_VERSION`
- [ ] `test -f scripts/check-upstream-release.cjs`
- [ ] `test -f .codex/skills/gsd-sync-upstream/SKILL.md`
- [ ] `node scripts/check-upstream-release.cjs --current-file get-shit-done/UPSTREAM_VERSION --latest-tag v999.0.0 --json`
- [ ] `node scripts/check-upstream-release.cjs --current-tag v1.28.0 --latest-tag v1.28.0 --json | rg -n "\"update_available\":false"`
- [ ] `rg -n "UPSTREAM_VERSION|tracked baseline|GitHub releases" docs/UPSTREAM-SYNC.md .codex/skills/gsd-sync-upstream/SKILL.md`
- [ ] `node get-shit-done/bin/gsd-tools.cjs validate health`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Maintainers can determine update/no-op state without mutating the repo
</success_criteria>

<output>
After completion, create `.planning/phases/07-automated-upstream-gsd-sync-skill/07-01-SUMMARY.md`
</output>
