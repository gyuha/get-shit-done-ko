---
phase: 04-validation-and-reporting
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - scripts/validation-reporting.cjs
  - skills/gsd-sync-upstream/scripts/validation-reporting.cjs
  - skills/gsd-sync-upstream/SKILL.md
  - docs/UPSTREAM-SYNC.md
  - docs/RELEASE-CHECKLIST.md
  - .planning/phases/04-validation-and-reporting/04-VALIDATION.md
  - tests/validation-reporting.test.cjs
autonomous: true
requirements:
  - RPT-01
  - RPT-02
  - VAL-03
must_haves:
  truths:
    - "Each validation run leaves behind one machine-readable report, one human-readable report, and stable latest pointers."
    - "Reports include release context, apply mode, command results, audit summary, verdict, caveats, and next actions."
    - "The maintainer sync skill and runbooks call the same helper and describe the same artifact names."
  artifacts:
    - path: "scripts/validation-reporting.cjs"
      provides: "JSON/Markdown report writer and latest pointer generation"
      exports: ["writeReportArtifacts", "renderMarkdownReport"]
    - path: "docs/UPSTREAM-SYNC.md"
      provides: "Maintainer runbook with the reporting helper in the canonical sync flow"
      contains: "node scripts/validation-reporting.cjs"
    - path: "skills/gsd-sync-upstream/SKILL.md"
      provides: "Skill instructions that call the reporting helper and mention report artifacts"
      contains: "validation-report-"
    - path: ".planning/phases/04-validation-and-reporting/04-VALIDATION.md"
      provides: "Updated Nyquist validation contract for the Phase 4 helper"
      contains: "tests/validation-reporting.test.cjs"
  key_links:
    - from: "scripts/validation-reporting.cjs"
      to: ".planning/phases/04-validation-and-reporting/latest.json"
      via: "stable latest machine-readable pointer"
      pattern: "latest\\.json"
    - from: "scripts/validation-reporting.cjs"
      to: ".planning/phases/04-validation-and-reporting/latest.md"
      via: "stable latest human-readable pointer"
      pattern: "latest\\.md"
    - from: "skills/gsd-sync-upstream/SKILL.md"
      to: "scripts/validation-reporting.cjs"
      via: "maintainer workflow invocation"
      pattern: "node scripts/validation-reporting\\.cjs"
---

<objective>
Finish Phase 4 by emitting the report artifacts and wiring the maintainer-facing skill and docs to the new helper so validation results are preserved and immediately actionable.

Purpose: Satisfy the reporting requirements and close the loop between code, runbooks, and execution-time verification.
Output: Timestamped JSON/Markdown reports, stable latest pointers, updated skill/runbook instructions, and an updated Phase 4 validation contract.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-validation-and-reporting/04-CONTEXT.md
@.planning/phases/04-validation-and-reporting/04-RESEARCH.md
@.planning/phases/04-validation-and-reporting/04-VALIDATION.md
@.planning/phases/04-validation-and-reporting/04-02-PLAN.md
@docs/UPSTREAM-SYNC.md
@docs/RELEASE-CHECKLIST.md
@skills/gsd-sync-upstream/SKILL.md
@scripts/validation-reporting.cjs
@tests/validation-reporting.test.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write JSON and Markdown validation report artifacts</name>
  <files>scripts/validation-reporting.cjs, skills/gsd-sync-upstream/scripts/validation-reporting.cjs, tests/validation-reporting.test.cjs</files>
  <read_first>scripts/validation-reporting.cjs, .planning/phases/04-validation-and-reporting/04-CONTEXT.md, .planning/phases/04-validation-and-reporting/04-VALIDATION.md, tests/validation-reporting.test.cjs</read_first>
  <behavior>
    - Test 1: a validation run writes `validation-report-<timestamp>.json` and `validation-report-<timestamp>.md` under `.planning/phases/04-validation-and-reporting/`.
    - Test 2: the helper refreshes `latest.json` and `latest.md` to mirror the newest run per D-13.
    - Test 3: the JSON and Markdown outputs are rendered from the same normalized run record, so verdict, caveats, and next actions stay aligned.
  </behavior>
  <action>Extend `scripts/validation-reporting.cjs` with `writeReportArtifacts(runRecord, options)` and `renderMarkdownReport(runRecord)`. Use `.planning/phases/04-validation-and-reporting` as the default report directory per D-11. Write timestamped files with the exact naming pattern `validation-report-YYYYMMDD-HHmmss.json` and `validation-report-YYYYMMDD-HHmmss.md`, then update stable pointer files `latest.json` and `latest.md`. Include these exact top-level JSON keys: `generated_at`, `release`, `apply`, `quick_validation`, `commands`, `audit`, `verdict`, `blocking_failures`, `caveats`, `next_actions`. Render Markdown with these exact headings: `# Validation Report`, `## Release Context`, `## Validation Commands`, `## Audit Summary`, `## Verdict`, `## Caveats`, and `## Next Actions`. Ensure `release` includes tracked baseline, latest release, latest published date, and package version; ensure `apply` includes `apply_mode`.</action>
  <acceptance_criteria>- `scripts/validation-reporting.cjs` contains the literal filenames `latest.json` and `latest.md`.\n- The helper writes `validation-report-` timestamped files under `.planning/phases/04-validation-and-reporting` by default.\n- The JSON report contains the keys `release`, `apply`, `commands`, `audit`, `verdict`, `caveats`, and `next_actions`.\n- The Markdown renderer contains the headings `## Release Context`, `## Validation Commands`, `## Audit Summary`, `## Verdict`, `## Caveats`, and `## Next Actions`.\n- The test file asserts both timestamped files and latest pointers.</acceptance_criteria>
  <verify>
    <automated>node --test tests/validation-reporting.test.cjs -x</automated>
  </verify>
  <done>Phase 4 produces stable decision artifacts in both machine-readable and maintainer-readable form without duplicating rendering logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the maintainer skill, runbooks, and validation contract to the reporting helper</name>
  <files>skills/gsd-sync-upstream/SKILL.md, docs/UPSTREAM-SYNC.md, docs/RELEASE-CHECKLIST.md, .planning/phases/04-validation-and-reporting/04-VALIDATION.md, scripts/validation-reporting.cjs, tests/validation-reporting.test.cjs</files>
  <read_first>skills/gsd-sync-upstream/SKILL.md, docs/UPSTREAM-SYNC.md, docs/RELEASE-CHECKLIST.md, .planning/phases/04-validation-and-reporting/04-VALIDATION.md, scripts/validation-reporting.cjs</read_first>
  <behavior>
    - Test 1: maintainer-facing docs call the same helper and mention the same report filenames as the implementation.
    - Test 2: Phase 4 validation strategy no longer shows the report test as missing Wave 0 scaffolding once the helper exists.
  </behavior>
  <action>Update `skills/gsd-sync-upstream/SKILL.md`, `docs/UPSTREAM-SYNC.md`, and `docs/RELEASE-CHECKLIST.md` to insert the new reporting step immediately after localization audit and canonical validation. Use the exact command `node scripts/validation-reporting.cjs --apply-json <apply_json> --audit-json <audit_json>` and add `--quick` only as the explicit opt-in path. In the docs, state that the helper writes `.planning/phases/04-validation-and-reporting/validation-report-<timestamp>.json`, `.md`, `latest.json`, and `latest.md`. Update `docs/RELEASE-CHECKLIST.md` so the actual apply command explicitly includes `--mode source-of-truth`, matching the Phase 2 contract. Update `.planning/phases/04-validation-and-reporting/04-VALIDATION.md` to mark `tests/validation-reporting.test.cjs` as existing, replace the Wave 0 placeholders with the real helper/report files, and set the task verification map to the actual test/report commands instead of `❌ W0` placeholders.</action>
  <acceptance_criteria>- `skills/gsd-sync-upstream/SKILL.md`, `docs/UPSTREAM-SYNC.md`, and `docs/RELEASE-CHECKLIST.md` each contain `node scripts/validation-reporting.cjs`.\n- The same files mention `latest.json` and `latest.md` or the `validation-report-<timestamp>` artifact pattern.\n- `docs/RELEASE-CHECKLIST.md` contains `node scripts/apply-upstream-refresh.cjs --to-tag <latest_tag> --mode source-of-truth`.\n- `.planning/phases/04-validation-and-reporting/04-VALIDATION.md` no longer marks `tests/validation-reporting.test.cjs` as `❌ W0`.\n- The validation test file still passes after the doc and contract updates.</acceptance_criteria>
  <verify>
    <automated>node --test tests/validation-reporting.test.cjs -x &amp;&amp; rg -n "node scripts/validation-reporting\\.cjs|latest\\.json|latest\\.md|--mode source-of-truth" skills/gsd-sync-upstream/SKILL.md docs/UPSTREAM-SYNC.md docs/RELEASE-CHECKLIST.md .planning/phases/04-validation-and-reporting/04-VALIDATION.md</automated>
  </verify>
  <done>The maintainer workflow, runbooks, and Phase 4 verification contract all reference the same helper, the same artifact names, and the same apply/validation ordering.</done>
</task>

</tasks>

<verification>
Run `node --test tests/validation-reporting.test.cjs -x`, then grep the maintainer docs for `node scripts/validation-reporting.cjs`, `latest.json`, `latest.md`, and `--mode source-of-truth` to confirm the prose stayed aligned with the code.
</verification>

<success_criteria>
Phase 4 ends with persistent validation reports, a single documented maintainer entrypoint, and no drift between the helper implementation and the human-facing workflow documentation.
</success_criteria>

<output>
After completion, create `.planning/phases/04-validation-and-reporting/04-03-SUMMARY.md`
</output>
