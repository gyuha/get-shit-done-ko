---
phase: 02-safe-apply-flow
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/apply-upstream-refresh.cjs
  - skills/gsd-sync-upstream/scripts/apply-upstream-refresh.cjs
  - tests/upstream-sync.test.cjs
  - skills/gsd-sync-upstream/SKILL.md
  - docs/UPSTREAM-SYNC.md
autonomous: true
requirements:
  - APPLY-01
user_setup: []
must_haves:
  truths:
    - Dry-run output exposes touched paths, preserved paths, overlay reapply, and overlay delete in a stable structured contract.
    - Repo-local and bundled apply helpers expose the same dry-run fields and text summary semantics.
    - Maintainer docs describe the exact dry-run fields before any apply step.
  artifacts:
    - path: scripts/apply-upstream-refresh.cjs
      provides: structured dry-run JSON/text summary for maintainer review
    - path: tests/upstream-sync.test.cjs
      provides: regression coverage for dry-run field names and summary invariants
    - path: skills/gsd-sync-upstream/SKILL.md
      provides: compare-first to dry-run review workflow with exact summary fields
  key_links:
    - from: scripts/apply-upstream-refresh.cjs
      to: tests/upstream-sync.test.cjs
      why: dry-run field names and summary semantics must remain regression-tested
    - from: skills/gsd-sync-upstream/SKILL.md
      to: docs/UPSTREAM-SYNC.md
      why: skill flow and runbook must describe the same dry-run review contract
---

<objective>
Lock the dry-run summary into a stable maintainer-facing contract before any real apply happens.

Purpose: Make upstream refresh review deterministic and safe by standardizing the fields maintainers inspect in dry-run output.
Output: Helper, tests, and docs aligned on the exact dry-run summary fields and wording.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-safe-apply-flow/02-RESEARCH.md

@scripts/apply-upstream-refresh.cjs
@skills/gsd-sync-upstream/scripts/apply-upstream-refresh.cjs
@tests/upstream-sync.test.cjs
@skills/gsd-sync-upstream/SKILL.md
@docs/UPSTREAM-SYNC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stabilize the dry-run summary contract in both helper locations</name>
  <files>scripts/apply-upstream-refresh.cjs, skills/gsd-sync-upstream/scripts/apply-upstream-refresh.cjs, tests/upstream-sync.test.cjs</files>
  <read_first>scripts/apply-upstream-refresh.cjs, skills/gsd-sync-upstream/scripts/apply-upstream-refresh.cjs, tests/upstream-sync.test.cjs</read_first>
  <action>Update both apply helpers so `buildDryRunResult()` and `formatDryRun()` expose the same explicit fields for maintainer review: `status`, `current_tag`, `incoming_tag`, `latest_published_at`, `package_version`, `summary`, `touched`, `preserved`, `overlay_reapply`, `overlay_delete`, `update_available`, and `no_op`. Keep the bundled helper byte-for-byte aligned with the repo-local helper. Add or tighten tests so dry-run update scenarios assert those exact field names and representative values, not just array presence.</action>
  <verify>node --test tests/upstream-sync.test.cjs</verify>
  <acceptance_criteria>
    - `tests/upstream-sync.test.cjs` asserts `touched`, `preserved`, `overlay_reapply`, and `overlay_delete` on the dry-run result object
    - `scripts/apply-upstream-refresh.cjs` still exports `buildDryRunResult` and `runRefresh`
    - `diff -u scripts/apply-upstream-refresh.cjs skills/gsd-sync-upstream/scripts/apply-upstream-refresh.cjs` exits 0
  </acceptance_criteria>
  <done>The helper contract for dry-run review is explicit, regression-tested, and mirrored in both helper locations.</done>
</task>

<task type="auto">
  <name>Task 2: Align maintainer dry-run guidance with the exact summary fields</name>
  <files>skills/gsd-sync-upstream/SKILL.md, docs/UPSTREAM-SYNC.md</files>
  <read_first>skills/gsd-sync-upstream/SKILL.md, docs/UPSTREAM-SYNC.md, scripts/apply-upstream-refresh.cjs</read_first>
  <action>Update the skill and runbook so the dry-run review step explicitly tells maintainers to inspect `incoming tag`, `touched paths`, `preserved paths`, `overlay reapply`, and `overlay delete` before approving apply. Keep the compare-first ordering intact and do not introduce any apply instruction before the dry-run summary fields are listed.</action>
  <verify>rg -n "incoming tag|touched paths|preserved paths|overlay reapply|overlay delete" skills/gsd-sync-upstream/SKILL.md docs/UPSTREAM-SYNC.md</verify>
  <acceptance_criteria>
    - `skills/gsd-sync-upstream/SKILL.md` contains all five dry-run review field names
    - `docs/UPSTREAM-SYNC.md` documents the same field names before the real apply command
  </acceptance_criteria>
  <done>Maintainers see the exact dry-run review contract in both the skill and the repo runbook.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test tests/upstream-sync.test.cjs` passes
- [ ] `diff -u scripts/apply-upstream-refresh.cjs skills/gsd-sync-upstream/scripts/apply-upstream-refresh.cjs` exits 0
- [ ] `rg -n "incoming tag|touched paths|preserved paths|overlay reapply|overlay delete" skills/gsd-sync-upstream/SKILL.md docs/UPSTREAM-SYNC.md` returns aligned wording
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Maintainers can inspect a stable dry-run summary before any real apply happens
  </success_criteria>

<output>
After completion, create `.planning/phases/02-safe-apply-flow/02-01-SUMMARY.md`
</output>
