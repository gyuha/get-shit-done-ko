---
phase: 04-validation-and-reporting
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - scripts/validation-reporting.cjs
  - skills/gsd-sync-upstream/scripts/validation-reporting.cjs
  - tests/validation-reporting.test.cjs
autonomous: true
requirements:
  - VAL-03
must_haves:
  truths:
    - "Validation results collapse into one deterministic verdict: pass, pass-with-caveats, partial, or fail."
    - "Structural and policy failures are distinguished from health and test failures so evidence collection follows the locked routing rules."
    - "Follow-up work is emitted as owner/severity-tagged next actions instead of free-form notes."
  artifacts:
    - path: "scripts/validation-reporting.cjs"
      provides: "Run-record normalization, verdict derivation, and next-action classification"
      exports: ["buildRunRecord", "deriveVerdict", "buildNextActions"]
    - path: "skills/gsd-sync-upstream/scripts/validation-reporting.cjs"
      provides: "Bundled mirror of verdict and action logic"
      contains: "pass-with-caveats"
    - path: "tests/validation-reporting.test.cjs"
      provides: "Regression coverage for verdict routing and next-action classification"
      contains: "deriveVerdict"
  key_links:
    - from: "scripts/validation-reporting.cjs"
      to: "scripts/audit-localization-gap.cjs"
      via: "overlay_missing / zh_cn_reintroduced / token_sensitive_candidates mapping"
      pattern: "overlay_missing|zh_cn_reintroduced|token_sensitive_candidates"
    - from: "scripts/validation-reporting.cjs"
      to: "scripts/apply-upstream-refresh.cjs"
      via: "preserved-path intrusion detection against PRESERVED_PATHS"
      pattern: "PRESERVED_PATHS"
    - from: "scripts/validation-reporting.cjs"
      to: "tests/validation-reporting.test.cjs"
      via: "verdict enums and next_action assertions"
      pattern: "pass-with-caveats|partial|fail"
---

<objective>
Add the verdict engine and failure-routing model on top of the validation entrypoint so the maintainer gets one deterministic answer plus actionable follow-up items.

Purpose: Implement D-07 through D-10 and D-15 through D-18 in code, not in reviewer interpretation.
Output: A normalized run record, the verdict derivation helpers, and regression tests for blocking failures, partial quick runs, caveats, and next actions.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-validation-and-reporting/04-CONTEXT.md
@.planning/phases/04-validation-and-reporting/04-RESEARCH.md
@.planning/phases/04-validation-and-reporting/04-VALIDATION.md
@.planning/phases/04-validation-and-reporting/04-01-PLAN.md
@docs/UPSTREAM-SYNC.md
@docs/RELEASE-CHECKLIST.md
@scripts/apply-upstream-refresh.cjs
@scripts/audit-localization-gap.cjs
@scripts/validation-reporting.cjs
@tests/validation-reporting.test.cjs

<interfaces>
From scripts/apply-upstream-refresh.cjs:
```javascript
module.exports = {
  PRESERVED_PATHS,
  formatDryRun,
  parseArgs,
  resolveApplyMode,
  runRefresh,
};
```

From scripts/audit-localization-gap.cjs:
```javascript
module.exports = {
  buildOverlayMissing,
  buildTokenSensitiveCandidates,
  buildTranslationCandidates,
  buildZhCnReintroduced,
  formatHuman,
  parseArgs,
  runAudit,
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize run records and derive the four-state verdict</name>
  <files>scripts/validation-reporting.cjs, skills/gsd-sync-upstream/scripts/validation-reporting.cjs, tests/validation-reporting.test.cjs</files>
  <read_first>scripts/validation-reporting.cjs, .planning/phases/04-validation-and-reporting/04-CONTEXT.md, .planning/phases/04-validation-and-reporting/04-RESEARCH.md, scripts/apply-upstream-refresh.cjs, scripts/audit-localization-gap.cjs, tests/validation-reporting.test.cjs</read_first>
  <behavior>
    - Test 1: full validation with all checks green and no findings yields `pass` per D-07.
    - Test 2: full validation with green commands plus non-blocking follow-up findings yields `pass-with-caveats` per D-09.
    - Test 3: quick validation that passes its allowed bundle yields `partial` and only `partial` per D-08.
    - Test 4: `validate consistency` failure, `roadmap analyze` failure, `zh_cn_reintroduced`, preserved-path intrusion, or explicit confirmed token drift yields `fail` per D-10 and D-16.
    - Test 5: `validate health` failure and test failure still keep collecting later findings before the final verdict is returned per D-17.
  </behavior>
  <action>Extend `scripts/validation-reporting.cjs` with `buildRunRecord`, `deriveVerdict`, and `hasPreservedPathIntrusion`. Define the only allowed verdict literals as `pass`, `pass-with-caveats`, `partial`, and `fail`. Model quick completion as `partial` only when `--quick` ran and all canonical checks plus the quick bundle completed; do not use `partial` for manual-review residue. Treat `validate consistency` failure, `roadmap analyze` failure, any `zh_cn_reintroduced` finding, any touched path that matches `PRESERVED_PATHS`, and any explicit `confirmed_token_sensitive` entry from the parsed audit JSON as blocking `fail` signals. Keep `validate health` failure and test failure in the run record, continue collecting findings, and let the final verdict resolve after all runnable checks finish. Mirror the same logic into `skills/gsd-sync-upstream/scripts/validation-reporting.cjs` and add explicit regression cases to `tests/validation-reporting.test.cjs` for all four verdict states.</action>
  <acceptance_criteria>- `scripts/validation-reporting.cjs` contains the exact verdict literals `pass`, `pass-with-caveats`, `partial`, and `fail`.\n- The helper has explicit logic for `validate consistency`, `roadmap analyze`, `zh_cn_reintroduced`, preserved-path intrusion, and `confirmed_token_sensitive`.\n- The test file includes one assertion per verdict state.\n- Health/test failures are recorded without short-circuiting later evidence collection.</acceptance_criteria>
  <verify>
    <automated>node --test tests/validation-reporting.test.cjs -x</automated>
  </verify>
  <done>The validation helper returns one deterministic verdict that matches the locked semantics, and the tests prove the routing logic for blocking vs non-blocking failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classify caveats and next actions with owner and severity</name>
  <files>scripts/validation-reporting.cjs, skills/gsd-sync-upstream/scripts/validation-reporting.cjs, tests/validation-reporting.test.cjs</files>
  <read_first>scripts/validation-reporting.cjs, scripts/audit-localization-gap.cjs, docs/RELEASE-CHECKLIST.md, tests/validation-reporting.test.cjs</read_first>
  <behavior>
    - Test 1: `overlay_missing` becomes a localization follow-up, not a blocking failure, unless another fail signal exists.
    - Test 2: `token_sensitive_candidates` becomes a maintainer verification caveat with concrete next actions.
    - Test 3: command/test failures produce next actions with owner and severity instead of free-form text.
  </behavior>
  <action>Add `buildNextActions(runRecord)` and classify follow-ups with these exact enums: owners `maintainer`, `localization`, `runtime`, `docs`; severities `blocker`, `major`, `minor`. Map `overlay_missing` to `owner: localization`, `severity: major`, and a next action that names each missing overlay file. Map `token_sensitive_candidates` to `owner: maintainer`, `severity: major`, and a next action that calls out command/path/placeholder/@ reference/manual markup verification. Map test failures to `owner: runtime`, `severity: blocker`. Map doc/runbook drift or checklist follow-up to `owner: docs`, `severity: minor`. Keep the caveat list separate from `blocking_failures` so `pass-with-caveats` remains distinct from `fail`. Extend `tests/validation-reporting.test.cjs` to assert the exact owner/severity enums and at least one concrete action message for overlay gaps and token-sensitive review.</action>
  <acceptance_criteria>- `scripts/validation-reporting.cjs` contains `owner: 'localization'`, `owner: 'maintainer'`, `owner: 'runtime'`, and `owner: 'docs'` mappings.\n- The helper contains `severity: 'blocker'`, `severity: 'major'`, and `severity: 'minor'` mappings.\n- `overlay_missing` and `token_sensitive_candidates` produce `next_actions` entries instead of only raw findings.\n- The test file asserts both owner and severity for localization and token-sensitive follow-up cases.</acceptance_criteria>
  <verify>
    <automated>node --test tests/validation-reporting.test.cjs -x</automated>
  </verify>
  <done>The helper emits owner/severity-tagged next actions and caveats that a maintainer can act on directly without reclassifying raw audit output by hand.</done>
</task>

</tasks>

<verification>
Run `node --test tests/validation-reporting.test.cjs -x` and then grep the helper for the four verdict literals and the owner/severity enums to ensure the public contract stayed literal and machine-checkable.
</verification>

<success_criteria>
The Phase 4 helper can now explain not just whether the sync preserved behavior, but why that verdict was chosen and what exact follow-up work remains.
</success_criteria>

<output>
After completion, create `.planning/phases/04-validation-and-reporting/04-02-SUMMARY.md`
</output>
