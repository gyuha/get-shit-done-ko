---
phase: 01-upstream-baseline-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github
  - .gitignore
  - .release-monitor.sh
  - CHANGELOG.md
  - LICENSE
  - README.md
  - README.zh-CN.md
  - SECURITY.md
  - agents
  - assets
  - bin
  - commands
  - docs
  - get-shit-done
  - hooks
  - package-lock.json
  - package.json
  - scripts
  - tests
  - .gitmodules
  - origin
autonomous: true
requirements:
  - SYNC-01
user_setup: []
must_haves:
  truths:
    - Upstream v1.28.0 repository contents exist directly in the project root
    - The project no longer depends on a nested origin submodule for runtime files
  artifacts:
    - package.json
    - get-shit-done/workflows/new-project.md
    - commands/gsd/new-project.md
    - scripts/run-tests.cjs
  key_links:
    - Root package.json points at bin/install.js
    - Root get-shit-done assets are present outside the former origin/ directory
---

<objective>
Flatten the local `origin/` submodule contents into the repository root and remove the submodule wiring.

Purpose: establish the exact upstream runtime tree in the main repository before any Korean localization work starts.
Output: upstream files and directories copied into the root, with `origin/` and `.gitmodules` removed.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-upstream-baseline-import/01-RESEARCH.md
@.gitmodules
@origin/README.md
@origin/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy tracked upstream tree from origin into the root</name>
  <files>.github, .gitignore, .release-monitor.sh, CHANGELOG.md, LICENSE, README.md, README.zh-CN.md, SECURITY.md, agents, assets, bin, commands, docs, get-shit-done, hooks, package-lock.json, package.json, scripts, tests</files>
  <read_first>.planning/PROJECT.md, .planning/phases/01-upstream-baseline-import/01-RESEARCH.md, origin/README.md, origin/package.json</read_first>
  <action>Copy the exact tracked top-level entries from `origin/` into the repository root: `.github`, `.gitignore`, `.release-monitor.sh`, `CHANGELOG.md`, `LICENSE`, `README.md`, `README.zh-CN.md`, `SECURITY.md`, `agents`, `assets`, `bin`, `commands`, `docs`, `get-shit-done`, `hooks`, `package-lock.json`, `package.json`, `scripts`, and `tests`. Preserve `.git/`, `.planning/`, `AGENTS.md`, and `CLAUDE.md` as local repository files. Use copy commands that preserve directory contents recursively and do not pull `.git` metadata out of `origin/`.</action>
  <verify>test -f package.json && test -d get-shit-done/workflows && test -d commands/gsd && test -d tests</verify>
  <acceptance_criteria>
    - `package.json` exists in the repository root
    - `get-shit-done/workflows/new-project.md` exists
    - `commands/gsd/new-project.md` exists
    - `scripts/run-tests.cjs` exists
  </acceptance_criteria>
  <done>All required upstream runtime files are present directly in the root tree.</done>
</task>

<task type="auto">
  <name>Task 2: Remove submodule wiring after the root import succeeds</name>
  <files>.gitmodules, origin</files>
  <read_first>.gitmodules, .planning/phases/01-upstream-baseline-import/01-RESEARCH.md, package.json</read_first>
  <action>After the root import is complete, remove the `origin` submodule from the repository. Deinitialize it if needed, remove the tracked gitlink entry, delete the `origin/` working tree, and remove `.gitmodules` so the repo no longer declares a submodule. Leave the imported root files intact.</action>
  <verify>test ! -e origin && test ! -e .gitmodules && node get-shit-done/bin/gsd-tools.cjs validate health</verify>
  <acceptance_criteria>
    - `origin` path does not exist
    - `.gitmodules` does not exist
    - `node get-shit-done/bin/gsd-tools.cjs validate health` exits 0
  </acceptance_criteria>
  <done>The repository no longer depends on the `origin` submodule and health validation still passes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f package.json`
- [ ] `test -d get-shit-done/workflows`
- [ ] `test ! -e origin && test ! -e .gitmodules`
- [ ] `node get-shit-done/bin/gsd-tools.cjs validate health`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Root tree now contains the upstream `v1.28.0` runtime layout without a nested submodule
</success_criteria>

<output>
After completion, create `.planning/phases/01-upstream-baseline-import/01-01-SUMMARY.md`
</output>
