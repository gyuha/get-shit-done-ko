---
phase: 06-rename-npm-package-and-installer-entrypoint-to-get-shit-done-ko
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - bin/install.js
  - get-shit-done/workflows/help.md
  - get-shit-done/workflows/update.md
autonomous: true
requirements:
  - PKG-01
  - PKG-02
user_setup: []
must_haves:
  truths:
    - The published npm package metadata and bin entrypoint use `get-shit-done-ko` rather than `get-shit-done-cc`
    - Installer help, help workflow, and update workflow all present `get-shit-done-ko` as the only canonical package command while preserving existing runtime flags
  artifacts:
    - package.json
    - package-lock.json
    - bin/install.js
    - get-shit-done/workflows/help.md
    - get-shit-done/workflows/update.md
  key_links:
    - package metadata and lockfile define the same package name and bin entry
    - installer help text and workflow help/update guidance show the same canonical `npx get-shit-done-ko@latest` usage
---

<objective>
Rename the machine-sensitive package identity surfaces first so the fork's actual npm entrypoint matches the intended public name before any documentation cleanup broadens the change set.

Purpose: make the package and runtime-facing guidance truthful at the source of publication.
Output: package metadata, installer help, and core help/update workflows all aligned to `get-shit-done-ko`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-rename-npm-package-and-installer-entrypoint-to-get-shit-done-ko/06-CONTEXT.md
@.planning/phases/06-rename-npm-package-and-installer-entrypoint-to-get-shit-done-ko/06-RESEARCH.md
@.planning/phases/06-rename-npm-package-and-installer-entrypoint-to-get-shit-done-ko/06-VALIDATION.md
@package.json
@package-lock.json
@bin/install.js
@get-shit-done/workflows/help.md
@get-shit-done/workflows/update.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename package metadata and bin entrypoint to get-shit-done-ko</name>
  <files>package.json, package-lock.json</files>
  <read_first>package.json, package-lock.json, .planning/phases/06-rename-npm-package-and-installer-entrypoint-to-get-shit-done-ko/06-CONTEXT.md, .planning/phases/06-rename-npm-package-and-installer-entrypoint-to-get-shit-done-ko/06-RESEARCH.md</read_first>
  <action>Implement locked decisions D-01, D-02, D-03, and D-07 by renaming the npm package identity from `get-shit-done-cc` to `get-shit-done-ko` in `package.json` and `package-lock.json`, including the published `bin` entry. Preserve version, file list, scripts, runtime flags, and every non-package identifier. Keep the existing dirty worktree intact by editing only the package-identity fields required for the rename.</action>
  <verify>node -e "const pkg=require('./package.json'); if(pkg.name!=='get-shit-done-ko') process.exit(1); if(pkg.bin['get-shit-done-ko']!=='bin/install.js') process.exit(1)" && node -e "const lock=require('./package-lock.json'); if(lock.name!=='get-shit-done-ko') process.exit(1); if(lock.packages[''].name!=='get-shit-done-ko') process.exit(1); if(lock.packages[''].bin['get-shit-done-ko']!=='bin/install.js') process.exit(1)" && npm pack --dry-run >/dev/null</verify>
  <acceptance_criteria>
    - `package.json` contains `"name": "get-shit-done-ko"`
    - `package.json` exposes `"get-shit-done-ko": "bin/install.js"` in `bin`
    - `package-lock.json` root package name and root `bin` entry both use `get-shit-done-ko`
  </acceptance_criteria>
  <done>The published package identity and CLI entrypoint are renamed at the npm metadata layer.</done>
</task>

<task type="auto">
  <name>Task 2: Align installer, help, and update workflows to the renamed package literal</name>
  <files>bin/install.js, get-shit-done/workflows/help.md, get-shit-done/workflows/update.md</files>
  <read_first>bin/install.js, get-shit-done/workflows/help.md, get-shit-done/workflows/update.md, package.json, .planning/phases/06-rename-npm-package-and-installer-entrypoint-to-get-shit-done-ko/06-CONTEXT.md</read_first>
  <action>Implement D-04, D-05, D-07, and D-08 by replacing canonical `get-shit-done-cc` package invocations in the installer help output and the `help` / `update` workflows with `get-shit-done-ko`. Preserve all runtime flags (`--claude`, `--codex`, `--global`, `--local`, `--uninstall`) and keep workflow semantics unchanged aside from the package literal.</action>
  <verify>rg -n "get-shit-done-ko" bin/install.js get-shit-done/workflows/help.md get-shit-done/workflows/update.md && ! rg -n "get-shit-done-cc" bin/install.js get-shit-done/workflows/help.md get-shit-done/workflows/update.md && node get-shit-done/bin/gsd-tools.cjs validate health</verify>
  <acceptance_criteria>
    - `bin/install.js` usage and example commands use `get-shit-done-ko`
    - `get-shit-done/workflows/help.md` shows `npx get-shit-done-ko@latest` in canonical update guidance
    - `get-shit-done/workflows/update.md` uses `get-shit-done-ko` for `npm view` and reinstall flows while keeping the same runtime flags
  </acceptance_criteria>
  <done>Runtime-facing installer and workflow guidance now agree with the renamed package identity.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node -e "const pkg=require('./package.json'); if(pkg.name!=='get-shit-done-ko') process.exit(1); if(pkg.bin['get-shit-done-ko']!=='bin/install.js') process.exit(1)"`
- [ ] `node -e "const lock=require('./package-lock.json'); if(lock.name!=='get-shit-done-ko') process.exit(1); if(lock.packages[''].name!=='get-shit-done-ko') process.exit(1); if(lock.packages[''].bin['get-shit-done-ko']!=='bin/install.js') process.exit(1)"`
- [ ] `npm pack --dry-run >/dev/null`
- [ ] `rg -n "get-shit-done-ko" bin/install.js get-shit-done/workflows/help.md get-shit-done/workflows/update.md`
- [ ] `! rg -n "get-shit-done-cc" bin/install.js get-shit-done/workflows/help.md get-shit-done/workflows/update.md`
- [ ] `node get-shit-done/bin/gsd-tools.cjs validate health`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Package metadata and runtime-facing install/update guidance point to the same canonical package name
</success_criteria>

<output>
After completion, create `.planning/phases/06-rename-npm-package-and-installer-entrypoint-to-get-shit-done-ko/06-01-SUMMARY.md`
</output>
