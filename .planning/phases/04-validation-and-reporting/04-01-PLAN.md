---
phase: 04-validation-and-reporting
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/validation-reporting.test.cjs
  - scripts/validation-reporting.cjs
  - skills/gsd-sync-upstream/scripts/validation-reporting.cjs
autonomous: true
requirements:
  - VAL-01
  - VAL-02
must_haves:
  truths:
    - "Maintainer can run the canonical validation sequence in the locked order from one helper entrypoint."
    - "Quick validation only runs when the maintainer explicitly opts in and the clean-gate passes."
    - "The quick bundle is fixed and includes compatibility tests plus sync-specific regression tests."
  artifacts:
    - path: "scripts/validation-reporting.cjs"
      provides: "Repo-local validation orchestration entrypoint"
      exports: ["parseArgs", "runValidationSequence", "QUICK_VALIDATION_TESTS"]
    - path: "skills/gsd-sync-upstream/scripts/validation-reporting.cjs"
      provides: "Bundled mirror of the validation orchestration helper"
      contains: "QUICK_VALIDATION_TESTS"
    - path: "tests/validation-reporting.test.cjs"
      provides: "Regression coverage for canonical ordering and quick-mode gating"
      contains: "validation reporting"
  key_links:
    - from: "scripts/validation-reporting.cjs"
      to: "get-shit-done/bin/gsd-tools.cjs"
      via: "validate health -> validate consistency -> roadmap analyze"
      pattern: "validate health|validate consistency|roadmap analyze"
    - from: "scripts/validation-reporting.cjs"
      to: "scripts/run-tests.cjs"
      via: "default full-suite execution"
      pattern: "scripts/run-tests\\.cjs"
    - from: "scripts/validation-reporting.cjs"
      to: "tests/upstream-sync.test.cjs"
      via: "fixed quick validation bundle"
      pattern: "tests/upstream-sync\\.test\\.cjs"
---

<objective>
Create the Phase 4 validation entrypoint and its test contract so the sync workflow can execute canonical validation and the fixed quick bundle without inventing new validation primitives.

Purpose: Implement D-01 through D-06 as executable code instead of prose-only runbook steps.
Output: A repo-local helper, a mirrored bundled helper, and regression tests for canonical ordering and quick-mode gating.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-validation-and-reporting/04-CONTEXT.md
@.planning/phases/04-validation-and-reporting/04-RESEARCH.md
@.planning/phases/04-validation-and-reporting/04-VALIDATION.md
@.planning/phases/02-safe-apply-flow/02-03-SUMMARY.md
@.planning/phases/03-localization-gap-audit/03-03-SUMMARY.md
@docs/UPSTREAM-SYNC.md
@docs/CLI-TOOLS.md
@skills/gsd-sync-upstream/SKILL.md
@scripts/apply-upstream-refresh.cjs
@scripts/audit-localization-gap.cjs
@scripts/check-upstream-release.cjs
@scripts/run-tests.cjs
@tests/helpers.cjs

<interfaces>
From scripts/check-upstream-release.cjs:
```javascript
module.exports = {
  buildReleaseState,
  compareVersions,
  fetchLatestRelease,
  formatHuman,
  normalizeTag,
  parseArgs,
  parseVersion,
  readCurrentTag,
  readPackageVersion,
  stripTagPrefix,
};
```

From scripts/apply-upstream-refresh.cjs:
```javascript
module.exports = {
  IMPORT_ENTRIES,
  PRESERVED_PATHS,
  buildDryRunResult,
  collectFiles,
  collectOverlayEntries,
  formatDryRun,
  getImportEntries,
  parseArgs,
  resolveApplyMode,
  runRefresh,
  updateTrackedBaselineFiles,
};
```

From scripts/audit-localization-gap.cjs:
```javascript
module.exports = {
  buildOverlayMissing,
  buildTokenSensitiveCandidates,
  buildTranslationCandidates,
  buildZhCnReintroduced,
  collectChangedFiles,
  formatHuman,
  parseArgs,
  runAudit,
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the validation orchestration test contract</name>
  <files>tests/validation-reporting.test.cjs</files>
  <read_first>tests/helpers.cjs, .planning/phases/04-validation-and-reporting/04-CONTEXT.md, .planning/phases/04-validation-and-reporting/04-VALIDATION.md</read_first>
  <behavior>
    - Test 1: default mode runs `validate health`, `validate consistency`, `roadmap analyze`, then `scripts/run-tests.cjs` in that exact order per D-01.
    - Test 2: `--quick` still runs the three canonical commands first and then only the fixed quick bundle per D-03 and D-04.
    - Test 3: quick mode is rejected unless audit findings are clean and the opt-in flag is present per D-02 and D-05.
    - Test 4: the quick bundle includes `tests/path-replacement.test.cjs`, `tests/runtime-converters.test.cjs`, `tests/codex-config.test.cjs`, `tests/antigravity-install.test.cjs`, `tests/copilot-install.test.cjs`, `tests/upstream-sync.test.cjs`, and `tests/localization-gap-audit.test.cjs` per D-06.
  </behavior>
  <action>Create `tests/validation-reporting.test.cjs` as the Wave 0 contract file. Use Node's built-in test runner and `node:assert`. Keep fixtures inline or as tiny local helpers inside this file; do not create a second reporting design that diverges from the future helper. Stub command execution at the function boundary so tests can assert exact command arrays, quick-gate rejection, and the fixed `QUICK_VALIDATION_TESTS` list without shelling out to the real full suite. Name the exported helper under test `runValidationSequence` and reserve `QUICK_VALIDATION_TESTS` as the exact constant name the implementation must expose.</action>
  <acceptance_criteria>- `tests/validation-reporting.test.cjs` exists and contains `describe('validation reporting'`.\n- The file asserts the exact canonical order `validate health`, `validate consistency`, `roadmap analyze`, `scripts/run-tests.cjs`.\n- The file asserts the exact quick bundle members `tests/upstream-sync.test.cjs` and `tests/localization-gap-audit.test.cjs`.\n- The file contains a rejection assertion for quick mode when clean-gate findings are present.</acceptance_criteria>
  <verify>
    <automated>test -f tests/validation-reporting.test.cjs &amp;&amp; rg -n "describe\\('validation reporting'|validate health|tests/upstream-sync\\.test\\.cjs|tests/localization-gap-audit\\.test\\.cjs" tests/validation-reporting.test.cjs</automated>
  </verify>
  <done>The reporting test contract exists, names the required public interface, and pins the canonical and quick validation behavior before implementation begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement canonical validation and quick-bundle orchestration</name>
  <files>scripts/validation-reporting.cjs, skills/gsd-sync-upstream/scripts/validation-reporting.cjs, tests/validation-reporting.test.cjs</files>
  <read_first>scripts/validation-reporting.cjs, skills/gsd-sync-upstream/SKILL.md, docs/UPSTREAM-SYNC.md, docs/CLI-TOOLS.md, scripts/apply-upstream-refresh.cjs, scripts/audit-localization-gap.cjs, tests/validation-reporting.test.cjs</read_first>
  <behavior>
    - Test 1: default execution returns a structured checks array in the canonical order from D-01.
    - Test 2: quick mode only runs when `--quick` is present and the clean-gate passes per D-02 and D-05.
    - Test 3: quick execution uses one fixed bundle from D-04 and D-06, not free-form test arguments.
  </behavior>
  <action>Create `scripts/validation-reporting.cjs` and mirror it to `skills/gsd-sync-upstream/scripts/validation-reporting.cjs`. Keep the implementation in CommonJS. Add `QUICK_VALIDATION_TESTS` with these exact paths in this exact order: `tests/path-replacement.test.cjs`, `tests/runtime-converters.test.cjs`, `tests/codex-config.test.cjs`, `tests/antigravity-install.test.cjs`, `tests/copilot-install.test.cjs`, `tests/upstream-sync.test.cjs`, `tests/localization-gap-audit.test.cjs`. Implement CLI flags `--quick`, `--apply-json <path>`, `--audit-json <path>`, `--json`, and `--cwd <path>`. Implement `runValidationSequence(options)` so it always runs `node get-shit-done/bin/gsd-tools.cjs validate health --raw`, `node get-shit-done/bin/gsd-tools.cjs validate consistency --raw`, and `node get-shit-done/bin/gsd-tools.cjs roadmap analyze --raw` before any test command. In default mode run `node scripts/run-tests.cjs`. In quick mode run `node --test` with the fixed bundle only when the parsed audit/apply JSON indicates `overlay_missing.length === 0`, `zh_cn_reintroduced.length === 0`, `token_sensitive_candidates.length === 0`, `status === 'ready'`, `no_op === false`, and no `touched` path falls under any `PRESERVED_PATHS` prefix. If the quick gate fails, exit non-zero with a message that names the specific failing signal instead of silently falling back to full mode.</action>
  <acceptance_criteria>- `scripts/validation-reporting.cjs` exports `parseArgs`, `runValidationSequence`, and `QUICK_VALIDATION_TESTS`.\n- The helper contains the exact quick bundle file list and the exact canonical command order from D-01 and D-06.\n- Quick mode requires `--quick`; no implicit quick path exists.\n- The bundled mirror file exists at `skills/gsd-sync-upstream/scripts/validation-reporting.cjs` and contains the same `QUICK_VALIDATION_TESTS` constant.</acceptance_criteria>
  <verify>
    <automated>node --test tests/validation-reporting.test.cjs -x</automated>
  </verify>
  <done>The repository has a tested validation entrypoint that enforces the locked execution policy and can be reused by the maintainer sync skill.</done>
</task>

</tasks>

<verification>
Run `node --test tests/validation-reporting.test.cjs -x` after implementation, then spot-check the helper source with `rg` to confirm the canonical command order and the fixed quick bundle remain literal strings in the file.
</verification>

<success_criteria>
Phase 4 now has a single helper entrypoint that executes D-01 through D-06 exactly, rejects unsafe quick runs, and exposes a stable contract for later verdict and report rendering work.
</success_criteria>

<output>
After completion, create `.planning/phases/04-validation-and-reporting/04-01-SUMMARY.md`
</output>
