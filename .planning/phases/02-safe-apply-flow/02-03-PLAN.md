---
phase: 02-safe-apply-flow
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - scripts/apply-upstream-refresh.cjs
  - skills/gsd-sync-upstream/scripts/apply-upstream-refresh.cjs
  - tests/upstream-sync.test.cjs
  - skills/gsd-sync-upstream/references/context.md
  - docs/UPSTREAM-SYNC.md
  - docs/RELEASE-CHECKLIST.md
autonomous: true
requirements:
  - APPLY-03
  - APPLY-04
user_setup: []
must_haves:
  truths:
    - Maintainers can opt into extending the default import surface without changing the default safe path.
    - Preserved local paths remain protected even when optional import entries are added.
    - Tests and docs make the extension path and preserve guardrails explicit.
  artifacts:
    - path: scripts/apply-upstream-refresh.cjs
      provides: opt-in import surface extension path layered on top of preserved local path protection
    - path: tests/upstream-sync.test.cjs
      provides: regression coverage for import extension and preserved-path safety
    - path: docs/RELEASE-CHECKLIST.md
      provides: maintainer validation steps for optional import-surface expansion
  key_links:
    - from: scripts/apply-upstream-refresh.cjs
      to: docs/RELEASE-CHECKLIST.md
      why: opt-in root expansion must be documented with the same guardrails as the helper
    - from: skills/gsd-sync-upstream/references/context.md
      to: scripts/apply-upstream-refresh.cjs
      why: preserved local paths policy must constrain any import-surface extension path
---

<objective>
Add a safe escape hatch for one-off import-surface expansion without weakening preserved local path protection.

Purpose: Let maintainers intentionally pull extra upstream root entries when needed, while keeping the default apply path conservative and protected.
Output: Helper, tests, and runbooks that support optional import extension under explicit preserve-aware guardrails.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-safe-apply-flow/02-RESEARCH.md
@.planning/phases/02-safe-apply-flow/02-02-PLAN.md

@scripts/apply-upstream-refresh.cjs
@skills/gsd-sync-upstream/scripts/apply-upstream-refresh.cjs
@tests/upstream-sync.test.cjs
@skills/gsd-sync-upstream/references/context.md
@docs/UPSTREAM-SYNC.md
@docs/RELEASE-CHECKLIST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an opt-in import-surface extension path without changing the default safe path</name>
  <files>scripts/apply-upstream-refresh.cjs, skills/gsd-sync-upstream/scripts/apply-upstream-refresh.cjs, tests/upstream-sync.test.cjs</files>
  <read_first>scripts/apply-upstream-refresh.cjs, skills/gsd-sync-upstream/scripts/apply-upstream-refresh.cjs, tests/upstream-sync.test.cjs, skills/gsd-sync-upstream/references/context.md</read_first>
  <action>Extend the apply helper CLI with a repeatable `--include-entry <path>` flag that appends extra root-level import entries to the default `IMPORT_ENTRIES` list for the current command only. Use the combined entry list consistently in dry-run collection, overlay diffing, and actual import. Keep the default behavior unchanged when no `--include-entry` flags are supplied. Add deterministic tests showing that an extra entry like `prompts` appears in `touched`, gets imported when present upstream, and does not change the default import set when omitted.</action>
  <verify>node --test tests/upstream-sync.test.cjs</verify>
  <acceptance_criteria>
    - `scripts/apply-upstream-refresh.cjs --help` mentions `--include-entry <path>`
    - `tests/upstream-sync.test.cjs` covers an `--include-entry` scenario with an extra root entry such as `prompts`
    - running without `--include-entry` still produces the original default import surface
  </acceptance_criteria>
  <done>The helper offers an explicit opt-in extension path without altering the default safe import surface.</done>
</task>

<task type="auto">
  <name>Task 2: Document extension guardrails and preserved path protection</name>
  <files>skills/gsd-sync-upstream/references/context.md, docs/UPSTREAM-SYNC.md, docs/RELEASE-CHECKLIST.md</files>
  <read_first>skills/gsd-sync-upstream/references/context.md, docs/UPSTREAM-SYNC.md, docs/RELEASE-CHECKLIST.md, scripts/apply-upstream-refresh.cjs</read_first>
  <action>Update the context reference and maintainer docs so they describe `--include-entry <path>` as an exceptional escape hatch for one-off upstream root imports. State concretely that preserved local paths `.planning/`, `AGENTS.md`, `CLAUDE.md`, `.codex/`, `.claude/`, and `.opencode/` remain protected even when extra entries are requested. Add checklist wording that maintainers must review the expanded `touched paths` set before apply.</action>
  <verify>rg -n -- "--include-entry <path>|\\.planning/|AGENTS.md|CLAUDE.md|expanded touched paths" skills/gsd-sync-upstream/references/context.md docs/UPSTREAM-SYNC.md docs/RELEASE-CHECKLIST.md</verify>
  <acceptance_criteria>
    - all three docs mention `--include-entry <path>`
    - preserved local paths are listed explicitly in at least one maintainer-facing doc
    - `docs/RELEASE-CHECKLIST.md` instructs maintainers to review the expanded touched paths before apply
  </acceptance_criteria>
  <done>The extension path is documented as opt-in and preserve-aware rather than as a new default.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test tests/upstream-sync.test.cjs` passes
- [ ] `node scripts/apply-upstream-refresh.cjs --help | rg -n -- "--include-entry <path>"` succeeds
- [ ] `rg -n -- "--include-entry <path>|\\.planning/|AGENTS.md|CLAUDE.md" skills/gsd-sync-upstream/references/context.md docs/UPSTREAM-SYNC.md docs/RELEASE-CHECKLIST.md` returns aligned wording
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Optional import-surface expansion exists without weakening preserved local path protection
  </success_criteria>

<output>
After completion, create `.planning/phases/02-safe-apply-flow/02-03-SUMMARY.md`
</output>
