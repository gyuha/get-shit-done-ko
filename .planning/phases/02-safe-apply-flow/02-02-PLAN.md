---
phase: 02-safe-apply-flow
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - scripts/apply-upstream-refresh.cjs
  - skills/gsd-sync-upstream/scripts/apply-upstream-refresh.cjs
  - tests/upstream-sync.test.cjs
  - skills/gsd-sync-upstream/SKILL.md
  - skills/gsd-sync-upstream/references/context.md
  - docs/UPSTREAM-SYNC.md
autonomous: true
requirements:
  - APPLY-02
user_setup: []
must_haves:
  truths:
    - Real apply uses source-of-truth mode as the default and reports that mode explicitly.
    - No-op and update_available gating stay intact when the apply mode becomes explicit.
    - Maintainer docs and skill text use the same apply mode name and command syntax as the helper.
  artifacts:
    - path: scripts/apply-upstream-refresh.cjs
      provides: explicit source-of-truth apply mode contract and default behavior
    - path: skills/gsd-sync-upstream/references/context.md
      provides: preserved-path and apply-mode policy for downstream maintainer flows
    - path: tests/upstream-sync.test.cjs
      provides: regression coverage for source-of-truth mode behavior
  key_links:
    - from: scripts/apply-upstream-refresh.cjs
      to: skills/gsd-sync-upstream/references/context.md
      why: helper mode semantics and maintainer policy must stay aligned
    - from: skills/gsd-sync-upstream/SKILL.md
      to: scripts/apply-upstream-refresh.cjs
      why: maintainer command examples must match the actual CLI contract
---

<objective>
Make source-of-truth apply the explicit default behavior of upstream refresh.

Purpose: Prevent ambiguous or accidental apply behavior by turning the existing default strategy into a named, documented CLI contract.
Output: Apply helper, tests, and maintainer references aligned around explicit source-of-truth mode.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-safe-apply-flow/02-RESEARCH.md
@.planning/phases/02-safe-apply-flow/02-01-PLAN.md

@scripts/apply-upstream-refresh.cjs
@skills/gsd-sync-upstream/scripts/apply-upstream-refresh.cjs
@tests/upstream-sync.test.cjs
@skills/gsd-sync-upstream/SKILL.md
@skills/gsd-sync-upstream/references/context.md
@docs/UPSTREAM-SYNC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make source-of-truth the explicit apply mode contract</name>
  <files>scripts/apply-upstream-refresh.cjs, skills/gsd-sync-upstream/scripts/apply-upstream-refresh.cjs, tests/upstream-sync.test.cjs</files>
  <read_first>scripts/apply-upstream-refresh.cjs, skills/gsd-sync-upstream/scripts/apply-upstream-refresh.cjs, tests/upstream-sync.test.cjs, get-shit-done/UPSTREAM_VERSION</read_first>
  <action>Extend the apply helper CLI so it accepts `--mode source-of-truth`, defaults to that mode when `--mode` is omitted, rejects unknown mode values, and includes `apply_mode: "source-of-truth"` in dry-run/apply results. Preserve the existing no-op guard, baseline snapshot diff, overlay reapply/delete, and tracked baseline update behavior. Mirror the same code in the bundled helper and add deterministic tests for default mode selection, explicit `--mode source-of-truth`, and unknown-mode failure.</action>
  <verify>node --test tests/upstream-sync.test.cjs</verify>
  <acceptance_criteria>
    - `scripts/apply-upstream-refresh.cjs --help` mentions `--mode <name>`
    - `tests/upstream-sync.test.cjs` asserts `apply_mode` equals `source-of-truth` for dry-run and apply scenarios
    - invoking the parser with an unknown mode throws `Unknown apply mode`
  </acceptance_criteria>
  <done>The helper exposes a concrete source-of-truth apply mode rather than relying on undocumented default behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Align maintainer instructions with the explicit source-of-truth mode</name>
  <files>skills/gsd-sync-upstream/SKILL.md, skills/gsd-sync-upstream/references/context.md, docs/UPSTREAM-SYNC.md</files>
  <read_first>skills/gsd-sync-upstream/SKILL.md, skills/gsd-sync-upstream/references/context.md, docs/UPSTREAM-SYNC.md, scripts/apply-upstream-refresh.cjs</read_first>
  <action>Update the skill, context reference, and runbook so the canonical real-apply command is `node scripts/apply-upstream-refresh.cjs --to-tag <latest_tag> --mode source-of-truth`. State explicitly that this mode reimports the tracked upstream surface, reapplies local overlays, deletes removed overlays, and keeps `get-shit-done/UPSTREAM_VERSION` as the baseline source of truth. Keep compare -> dry-run -> apply ordering unchanged.</action>
  <verify>rg -n "source-of-truth|--mode source-of-truth|reapplies local overlays|UPSTREAM_VERSION" skills/gsd-sync-upstream/SKILL.md skills/gsd-sync-upstream/references/context.md docs/UPSTREAM-SYNC.md</verify>
  <acceptance_criteria>
    - all three docs contain `--mode source-of-truth`
    - at least one maintainer-facing doc explicitly says the mode reimports the tracked upstream surface and reapplies local overlays
  </acceptance_criteria>
  <done>Maintainer instructions and helper behavior share the same explicit source-of-truth apply contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --test tests/upstream-sync.test.cjs` passes
- [ ] `node scripts/apply-upstream-refresh.cjs --help | rg -n -- "--mode <name>"` succeeds
- [ ] `rg -n "source-of-truth|--mode source-of-truth" skills/gsd-sync-upstream/SKILL.md skills/gsd-sync-upstream/references/context.md docs/UPSTREAM-SYNC.md` returns aligned wording
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Source-of-truth apply is an explicit, documented, and regression-tested default mode
  </success_criteria>

<output>
After completion, create `.planning/phases/02-safe-apply-flow/02-02-SUMMARY.md`
</output>
